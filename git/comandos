$ git config --global user.name "angelicamp"

$ git config --global user.email "meu@email.com"

$ git init

$ git add . : adiciona todos os arquivos ao projeto

$ git commit -a -m "primeiro commit"

$ git remote add origin https://github.com/angelicamp/livraria-alura.git : nesse caso passa o endereço disponivel pra clone

$ git remote set-url : usado para alterar o endereço

$ git push origin main

$ git branch --list : lista as branches

$ git remote set-url 
